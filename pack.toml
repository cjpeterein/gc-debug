[pack]
name = "gc-debug"
version = "0.1.0"
schema = 2
description = "Trace complex Gas* (gc, bd/beads, dolt, go-mysql-server, doltlite) issues to root cause across the dependency stack, prove the fix on real data, and land a mergeable upstream PR."
